<div class="container">
<div class="inbox-title">
    <h1> <%= @inbox.topic %></h1>
  </div>

  <% @inbox.messages.each do |message| %>
<div class="message-container">
  <div class="inbox-message">
    <i class="author">
    <span><%= message.user.first_name %></span>
    <small><%= message.created_at.strftime("at %k:%M on the %m/%e") %></small>
    </i>
    <p><%= message.content %></p>
  </div>
</div>
  <% end %>
</div>
<%# simple_form_for [ @inbox, @message ], remote: true do |f| %>
  <%# f.input :content, label: false, placeholder: "Message ##{@inbox.topic}" %>
<%# end %>
